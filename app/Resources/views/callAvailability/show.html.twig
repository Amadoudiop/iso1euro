{% extends 'base.html.twig' %}

{% block body %}
    <h1>Callavailability</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ callAvailability.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ callAvailability.label }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('callavailability_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('callavailability_edit', { 'id': callAvailability.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
