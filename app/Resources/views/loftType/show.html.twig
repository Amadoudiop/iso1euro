{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lofttype</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ loftType.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ loftType.label }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('lofttype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('lofttype_edit', { 'id': loftType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
